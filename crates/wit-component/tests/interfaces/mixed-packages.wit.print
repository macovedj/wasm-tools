package foo:bar;



package another:thing {
  /// documenting an interface
  interface something {
    @since(version = 1.2.3)
    record my-record {
      foo: string,
    }
  }

  world another-world {
    export something;
  }
}

package third:pkg {
  @since(version = 1.2.3)
  interface things {
    /// documenting a type
    record other-record {
      foo: string,
    }
  }

}

world this-world {
  import another:thing/something;
  import third:pkg/things;

  export another:thing/something;
}

